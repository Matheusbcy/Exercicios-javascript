const numAleatorio = Math.floor(Math.random()*10)
const numUsuario = 5

if(numAleatorio === numUsuario){
    console.log(`Números são iguais os numeros foram ${numAleatorio} e ${numUsuario}`);
} else {
    console.log(`Números não são iguais os numeros foram ${numAleatorio} e ${numUsuario}`)
}