const idade = 19
const altura = 1.90
const problemaCardiaco = true

if (idade > 18 && altura > 1.60 && problemaCardiaco === false) {
    console.log('Pode entrar no parque!')
} else {
    console.log('Não pode entrar no parque! :(');
}